# GymGymGym 验收清单

详细手动步骤见 `docs/07-manual-test-plan.md`。

## 1. 仓库

- [x] GitHub remote 指向 `https://github.com/wangkaiyu001/GymGymGym.git`
- [x] 所有代码和文档已 commit
- [x] 所有代码和文档已 push
- [x] `external/`、`dist/`、密钥和个人凭证没有被提交
- [x] `npm run check` 通过
- [x] `npm test` 通过
- [x] `git diff --check` 通过

## 2. 小程序

- [x] 微信官方 `miniprogram-ci` 编译全部页面成功
- [x] AppID 已替换为 `wxe5658bb09f7c33f9`
- [x] `wx.cloud.init` 使用 `code-realtime-d7gbuxrbze297e600`
- [x] 0.1.0 代码已上传到微信公众平台
- [ ] Tab 能正常切换
- [ ] 今日训练页能创建训练
- [ ] 能添加普通组
- [ ] 能添加 2、3、4 动作超级组，且同一动作不会重复加入同一超级组
- [ ] 能填写训练场景：目标部位、可用器材、训练策略、身体状态、备注
- [ ] 根据训练场景能显示推荐动作
- [ ] 完成过带目标部位的训练后，训练页能显示该部位距上次训练的间隔天数
- [ ] 推荐动作点击“加入”后能创建普通训练块
- [ ] 能保存训练
- [ ] 所有组都为空时不能完成训练，草稿不出现在最近训练或档案统计中
- [ ] “放弃训练”能删除本地编辑区和云端草稿的 session/block/set
- [ ] 退出后重新进入训练页能恢复最近草稿，重试保存不会重复写入已保存组
- [ ] 最近训练能复制为新的训练编辑区
- [ ] 最近训练能按“全部 / 同目标 / 同场所 / 同目标+场所”筛选复制来源
- [ ] 训练详情能编辑标题、日期、场所、目标、备注
- [ ] 训练详情能编辑已保存组的重量、次数、RPE、热身/力竭
- [ ] 训练详情能删除误录训练组
- [ ] 训练详情能删除误录训练块
- [ ] 动作库能搜索和筛选
- [ ] 动作库能收藏/取消收藏动作
- [ ] 动作库“只看收藏”能只显示收藏动作
- [ ] 训练动作选择器能优先显示“常用收藏”
- [ ] 个人档案能展示统计
- [ ] 个人档案能展示近 7 天 / 近 30 天训练摘要
- [ ] 个人档案能展示 PR 关注 Top 3
- [ ] 个人档案能导出当前用户 JSON 备份并调起微信文件分享
- [ ] 编辑或删除训练组后，个人档案统计能重算并更新
- [ ] 目标设置能保存用户配置
- [ ] 目标页能创建重量、1RM、周训练次数、目标体重或自定义指标目标
- [ ] 具体目标能关联动作、重点部位和截止日期
- [ ] 具体目标能标记完成、恢复进行中和删除

## 3. CloudBase

- [x] CloudBase MCP 已登录并绑定 `code-realtime-d7gbuxrbze297e600`（CLI 旧登录态可忽略）
- [x] 云函数 `getUserContext` 已部署并确认为 `Active`
- [x] 云函数 `recalculateStats` 已部署并确认为 `Active`
- [x] 7 个数据库集合均已创建
- [x] 安全规则已配置为 `CUSTOM` 并回读验证
- [x] 用户数据写规则不包含允许任意新文档的 `!doc._openid` 分支
- [x] 创建规则校验 `request.data.user_openid == auth.openid`，更新/删除规则校验 `doc._openid == auth.openid`
- [x] `exercise_stats` 禁止小程序客户端直接写入

## 4. 数据

- [x] seed 动作数据可用
- [x] 完整动作库已通过脚本标准化为 1324 条动作
- [x] 完整动作库导入脚本 dry-run 通过
- [x] 完整动作库已完成小批量写入验证
- [x] 完整动作库已全量导入 `exercises`（线上 1325 条含 1 条 marker）
- [x] 训练数据按用户隔离的客户端与安全规则边界已实现
- [x] 训练列表、训练详情、训练组和档案查询显式限定当前 OpenID
- [x] 训练、训练块、训练组和目标的更新/删除均使用文档 ID + 当前 OpenID 条件
- [x] `user_goals` 查询显式限定当前 OpenID
- [x] 超过单页上限的训练、训练块、训练组和目标使用分页读取并写入备份

## 5. 用户本人必须执行

- [x] 提供真实微信小程序 AppID
- [ ] 在微信开发者工具导入项目
- [ ] 绑定小程序和 CloudBase 环境
- [x] 当前公网 IP 已加入微信小程序代码上传 IP 白名单
- [ ] 如需要手动使用 CLI，再执行 `tcb login`；日常 Codex 部署可使用当前已认证的 CloudBase MCP
- [ ] 如要公开发布，确认 Gym visual 媒体授权

## 6. 当前已知限制 / 不作为阻塞项

- [ ] 场景化推荐是轻量规则版；后续可接入历史表现趋势和疲劳间隔。
- [ ] PR 关注目前展示当前 Top 3，不是完整 PR 时间线；后续可扩展为按动作查看历史突破记录。
- [ ] 当前面向两人私用，未实现公开多租户后台、课程市场或社交功能。

## 7. V0.2 聚焦版

- [x] 删除“创建具体目标”，只保留基础档案和习惯
- [x] 档案仅保留近 7/30 天摘要与 PR
- [x] 删除 JSON 导出、累计数字和动作表现长列表
- [x] 训练页改为自然语言输入并自动生成动作、组数、次数、RPE 和历史参考重量
- [x] 可选择过去日期补录训练
- [x] 开始训练后聚焦实际重量、次数和 RPE
- [x] 动作库分页浏览全部 1324 个动作
- [x] 1324 张开源动作示范图已上传 CloudBase 云存储
- [x] 1324 张示范图已同步至 CloudBase 静态托管
- [x] 微信开发版本 `0.2.0` 已上传成功
- [ ] 在 CloudBase 环境设置中关联小程序 AppID `wxe5658bb09f7c33f9`
